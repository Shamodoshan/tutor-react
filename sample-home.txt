import React, { useRef } from "react";
import { motion, useScroll, useTransform, useSpring, useInView } from "framer-motion";
import { 
  Zap, ArrowRight, Play, MessageCircle, 
  Cpu, Layout, ShieldCheck, PhoneCall 
} from "lucide-react";
import { Button } from "@/shared/ui/button";

// --- Advanced Animation Hooks ---
const RevealOnScroll = ({ children }: { children: React.ReactNode }) => {
  const ref = useRef(null);
  const isInView = useInView(ref, { once: true, margin: "-100px" });

  return (
    <motion.div
      ref={ref}
      initial={{ opacity: 0, y: 50, scale: 0.95 }}
      animate={isInView ? { opacity: 1, y: 0, scale: 1 } : {}}
      transition={{ duration: 0.8, ease: [0.16, 1, 0.3, 1] }}
    >
      {children}
    </motion.div>
  );
};

export const Home = () => {
  const containerRef = useRef(null);
  const { scrollYProgress } = useScroll({
    target: containerRef,
    offset: ["start start", "end end"]
  });

  const smoothProgress = useSpring(scrollYProgress, { stiffness: 100, damping: 30 });
  const heroOpacity = useTransform(smoothProgress, [0, 0.2], [1, 0]);
  const heroScale = useTransform(smoothProgress, [0, 0.2], [1, 0.9]);

  return (
    <div ref={containerRef} className="relative min-h-screen bg-[#F9FAFB] text-[#0F172A] overflow-x-hidden">
      
      {/* --- Sticky High-Visibility Nav --- */}
      <nav className="fixed top-0 left-0 right-0 z-[100] p-6 flex justify-center pointer-events-none">
        <motion.div 
          className="bg-white/90 backdrop-blur-xl border border-slate-200 shadow-2xl rounded-3xl px-8 py-4 flex justify-between items-center w-full max-w-5xl pointer-events-auto"
          initial={{ y: -100 }} animate={{ y: 0 }}
        >
          <div className="flex flex-col">
            <h3 className="font-black text-xl tracking-tighter leading-none">AKILA EKANAYAKE</h3>
            <span className="text-[10px] font-black text-rose-600 uppercase tracking-widest">Physics Faculty</span>
          </div>
          <div className="hidden lg:flex gap-10 items-center">
            {['Methodology', 'Results', 'Portal'].map((item) => (
              <a key={item} href={`#${item.toLowerCase()}`} className="text-[11px] font-bold uppercase tracking-widest text-slate-400 hover:text-rose-600 transition-colors">
                {item}
              </a>
            ))}
            <Button className="bg-[#0F172A] hover:bg-rose-600 text-white rounded-xl px-8 font-bold transition-all shadow-lg">
              ENROLL 2026
            </Button>
          </div>
        </motion.div>
      </nav>

      {/* --- Hero: Perspective 3D Stage --- */}
      <motion.section 
        style={{ opacity: heroOpacity, scale: heroScale }}
        className="relative h-screen flex flex-col items-center justify-center text-center px-6 pt-20"
      >
        <div className="absolute inset-0 bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))] from-rose-100/40 via-transparent to-transparent -z-10" />
        
        <RevealOnScroll>
          <div className="mb-10 inline-flex items-center gap-3 bg-white border border-slate-200 px-5 py-2 rounded-2xl shadow-sm">
            <span className="w-2 h-2 rounded-full bg-rose-500 animate-pulse" />
            <span className="text-[10px] font-black uppercase tracking-[0.2em] text-slate-500">BSc (Electronic) | MBA (UOWL)</span>
          </div>

          <h1 className="text-[clamp(4rem,12vw,9rem)] font-[1000] tracking-[-0.06em] leading-[0.85] text-[#0F172A] mb-12">
            PHYSICS <br />
            <span className="text-transparent bg-clip-text bg-gradient-to-r from-rose-600 to-rose-400 italic">REMASTERED.</span>
          </h1>

          <p className="text-xl md:text-2xl text-slate-400 max-w-2xl mx-auto font-medium leading-relaxed mb-12">
            Experience the most interactive A/L Physics curriculum in Sri Lanka. 
            Designed for rank-seekers.
          </p>

          <div className="flex flex-col sm:flex-row gap-6 justify-center">
            <Button size="lg" className="h-16 px-12 bg-rose-600 hover:bg-[#0F172A] text-white rounded-2xl text-xl font-black shadow-2xl shadow-rose-200 transition-all">
              SECURE YOUR SEAT
            </Button>
            <Button variant="ghost" size="lg" className="h-16 px-12 rounded-2xl text-xl font-bold border-2 border-slate-200 hover:bg-white">
              <Play className="mr-3 fill-rose-600 text-rose-600" /> VIRTUAL TOUR
            </Button>
          </div>
        </RevealOnScroll>
      </motion.section>

      {/* --- Section 2: Interactive 3D Features (The Core Pillars) --- */}
      <section id="methodology" className="py-40 px-6 max-w-7xl mx-auto">
        <div className="grid lg:grid-cols-12 gap-12 items-center">
          <div className="lg:col-span-5 space-y-8">
            <h2 className="text-5xl font-black tracking-tighter leading-tight">A Platform Built <br/>for Excellence.</h2>
            <p className="text-lg text-slate-500 font-medium leading-relaxed">
              We combined the intensity of the physical classroom with the precision of AI-driven digital analytics.
            </p>
            <div className="space-y-4">
              <CheckItem text="Automated Performance Prediction" />
              <CheckItem text="4K Multi-cam Live Streams" />
              <CheckItem text="Digital 'Golden Paper' Feedback" />
            </div>
          </div>

          <div className="lg:col-span-7 grid md:grid-cols-2 gap-6 perspective-1000">
            <FeatureCard 
              icon={<Cpu />} 
              title="Smart LMS" 
              desc="Your personalized dashboard for recordings & notes." 
              rotate="-2deg"
            />
            <FeatureCard 
              icon={<Layout />} 
              title="Module Theory" 
              desc="Step-by-step branding theory for zero-level starts." 
              rotate="2deg"
              active
            />
          </div>
        </div>
      </section>

      {/* --- Section 3: The Call to Action (Physical Branding) --- */}
      <section className="py-32 px-6">
        <RevealOnScroll>
          <div className="max-w-6xl mx-auto bg-[#0F172A] rounded-[4rem] p-12 md:p-32 relative overflow-hidden text-center shadow-[0_50px_100px_rgba(0,0,0,0.2)]">
            <div className="relative z-10 space-y-10">
              <h2 className="text-5xl md:text-8xl font-black text-white tracking-tighter leading-none">
                JOIN THE <br/><span className="text-rose-600">FACULTY.</span>
              </h2>
              <p className="text-slate-400 text-xl max-w-xl mx-auto font-medium">
                Physical sessions starting soon at Montana Gampaha. Limited enrollment.
              </p>
              <div className="flex flex-wrap justify-center gap-6">
                <Button className="bg-white text-black hover:bg-rose-600 hover:text-white h-16 px-12 rounded-2xl text-xl font-black transition-all">
                  <PhoneCall className="mr-3" /> 076 582 5606
                </Button>
                <Button className="bg-rose-600 text-white h-16 px-12 rounded-2xl text-xl font-black shadow-xl shadow-rose-900/40">
                  WHATSAPP REGISTRATION
                </Button>
              </div>
            </div>
            {/* Background 3D Elements */}
            <div className="absolute top-0 right-0 w-96 h-96 bg-rose-600/20 blur-[150px] -mr-20 -mt-20" />
            <div className="absolute bottom-0 left-0 w-96 h-96 bg-blue-600/10 blur-[150px] -ml-20 -mb-20" />
          </div>
        </RevealOnScroll>
      </section>

      {/* --- Minimalist Footer --- */}
      <footer className="py-20 flex flex-col items-center gap-8 border-t border-slate-100">
        <div className="flex gap-12 text-[10px] font-black uppercase tracking-[0.4em] text-slate-400">
          <a href="#" className="hover:text-rose-600 transition-colors">Facebook</a>
          <a href="#" className="hover:text-rose-600 transition-colors">YouTube</a>
          <a href="#" className="hover:text-rose-600 transition-colors">Telegram</a>
        </div>
        <p className="text-[10px] font-bold text-slate-300">© 2026 AKILA EKANAYAKE PHYSICS HUB</p>
      </footer>

      {/* Floating Magnetic CTA */}
      <motion.div 
        className="fixed bottom-10 right-10 z-[100]"
        whileHover={{ scale: 1.1 }}
      >
        <button className="bg-rose-600 text-white w-20 h-20 rounded-[2rem] flex items-center justify-center shadow-2xl shadow-rose-300 transition-all hover:bg-[#0F172A]">
          <MessageCircle size={32} />
        </button>
      </motion.div>
    </div>
  );
};

// --- Atomic Animated Components ---

const CheckItem = ({ text }: { text: string }) => (
  <div className="flex items-center gap-4 group">
    <div className="w-6 h-6 rounded-full bg-rose-50 flex items-center justify-center text-rose-600 border border-rose-100">
      <ShieldCheck size={14} />
    </div>
    <span className="text-sm font-bold text-slate-600 group-hover:text-[#0F172A] transition-colors">{text}</span>
  </div>
);

const FeatureCard = ({ icon, title, desc, rotate, active }: any) => (
  <motion.div 
    whileHover={{ scale: 1.05, rotate: 0, z: 50 }}
    style={{ rotate }}
    className={`p-10 rounded-[3rem] transition-all duration-500 border ${
      active 
      ? 'bg-white border-white shadow-[0_40px_100px_rgba(0,0,0,0.08)] z-20' 
      : 'bg-slate-50 border-slate-200/50 shadow-sm z-10'
    }`}
  >
    <div className={`w-16 h-16 rounded-2xl flex items-center justify-center mb-10 ${active ? 'bg-rose-600 text-white shadow-xl shadow-rose-200' : 'bg-white text-slate-400'}`}>
      {React.cloneElement(icon as React.ReactElement, { size: 32 })}
    </div>
    <h3 className="text-2xl font-black mb-4 tracking-tight">{title}</h3>
    <p className="text-slate-500 font-medium leading-relaxed">{desc}</p>
    <div className="mt-8 pt-8 border-t border-slate-100">
      <span className="text-[10px] font-black uppercase tracking-widest text-rose-600 flex items-center gap-2 cursor-pointer group">
        Explore Module <ArrowRight size={14} className="group-hover:translate-x-2 transition-transform" />
      </span>
    </div>
  </motion.div>
);